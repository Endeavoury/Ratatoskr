# G8 accounting-design routing

ACTIVE ROLE: protocol-orchestrator

## Scope

Route exactly one protocol-api-designer corrective design assignment for the blocked DNS G8 remediation authority question. This routing record owns no technical conclusion, production implementation, test, vector, or gate approval.

## Verified baseline

- Repository root / command cwd: `/home/hermes/hermes-workspace/projects/Ratatoskr`
- Branch: `hermes/dns-implementation-20260913`
- Local and `origin/hermes/dns-implementation-20260913`: `673db120e9d93d8dba1cc71c66a40de59225f436` before this routing record.
- Required Git wrapper: `/home/hermes/hermes-workspace/.hermes-control/integrations/github/git-agent.sh` (executable and used with the role argument).
- No live specialist/delegation process was present; historical `IN_PROGRESS`, `CHANGES_REQUESTED`, and `BLOCKED` records were treated as completed artifacts, not live work.

## Routing decision

The last corrective c-protocol-implementer assignment returned `BLOCKED` because a safe replacement for the global request tracker requires a context-owned lifecycle design and exact core-internal write authority. Per the security handoff and role routing contract, ownership/lifetime safety returns to `protocol-api-designer` before a renewed implementation assignment. The single leaf receives a read-only design assignment limited to its unique workspace and the blocked handoff's Resolution section.

## Non-advancement

G8 remains `CHANGES_REQUESTED`; G7 is not approved for a future corrective candidate, and G9/later stages are not routed. No existing technical gate is claimed as passed by this record.